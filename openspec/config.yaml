schema: spec-driven
context: |
  This repo is epg.monster studio: Rust + TypeScript + Tauri v2.
  The window title stays "epg.monster studio". Never invent features, never simplify
  controls, never restyle tokens. Living requirements are in openspec/specs/.
  GPL-3.0. Do not log access keys or provider stream URLs.
