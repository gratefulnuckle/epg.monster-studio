# GitHub mapping

epic: 8

| task | issue |
|------|-------|
| Verify `.studio-install.json` is written on `--install` with how/path/scoopName/wingetId | 9 |
| Verify `--uninstall` uses that JSON and prompts for Scoop `nodejs` as well as LTS | 10 |
| Verify Refresh-Path cannot grow past a unique Machine+User+extras set | 11 |
| Verify two-pane install UI does not wrap step rows or drop the header | 12 |
| Verify `npm run build` uses local TypeScript (no global `tsc`) | 13 |
