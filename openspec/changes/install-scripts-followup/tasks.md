# Tasks

- [x] Verify `.studio-install.json` is written on `--install` with how/path/scoopName/wingetId
- [x] Verify `--uninstall` uses that JSON and prompts for Scoop `nodejs` as well as LTS
- [x] Verify Refresh-Path cannot grow past a unique Machine+User+extras set
- [x] Verify two-pane install UI does not wrap step rows or drop the header
- [x] Verify `npm run build` uses local TypeScript (no global `tsc`)
