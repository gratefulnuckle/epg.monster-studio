# Keep Windows Rust artifacts off C:. studio.sh overrides this with
# CARGO_TARGET_DIR on Linux/macOS — cargo treats a drive letter as a
# relative path, which is why --shortcuts could not find the binary.
[build]
target-dir = "S:\\toolchains\\cargo-target\\epg-monster-studio"
